#include <stdio.h>
#include <stdint.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <math.h>
#include <ctype.h>
#include <time.h>

#ifdef LOCAL
#define eprintf(...) fprintf(stderr, __VA_ARGS__), fflush(stderr)
#else
#define eprintf(...) (void) 0
#endif

int main() {
  const double eps = 1e-9;
  int n;
  scanf("%d", &n);
  while (n--) {
    double x;
    char d, c, p;
    scanf("%lf %c %c %c", &x, &d, &c, &p);
    x = 
  return 0;
}
